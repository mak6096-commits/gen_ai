# Production-ready dependencies for Orders & Inventory Microservice
# Tested compatible versions that work with minimal_app.py

fastapi==0.60.0
pydantic==1.7.4
uvicorn==0.12.3

# Additional testing dependencies (optional)
requests==2.31.0

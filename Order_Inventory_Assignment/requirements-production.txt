# Minimal production requirements for Render.com deployment
# These versions are tested and known to work without Rust compilation

fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
sqlmodel==0.0.14

# Alternative: Use older compatible versions if issues persist
# fastapi==0.100.0
# uvicorn==0.23.0
# pydantic==2.0.0
# sqlmodel==0.0.8
